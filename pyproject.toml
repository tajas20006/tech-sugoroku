[project]
name = "tech-sugoroku"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flet[all]>=0.86.5",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.1",
    "pyinstaller>=6.21.0",
    "pytest>=9.1.1",
    "pytest-cov>=7.1.0",
    "ruff>=0.16.1",
]
