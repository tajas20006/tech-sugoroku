# AGENTS.md

## このリポジトリについて

`tech-sugoroku` は、AWS を学べる2人対戦の GUI すごろくゲームです。
ゲームルールは `game.py`、Flet の表示と演出は `main.py`、クイズとイベント文言は
`assets/`、実装仕様は `specs/mvp-game/` に置きます。

## 必須の開発手順

機能追加・仕様変更・バグ修正では、必ず次の順序を守ります。

1. 実装前に `specs/mvp-game/spec.md` を更新する。必要に応じて
   `context.md`、`plan.md`、`progress.md` も更新する。
2. `code-assist` スキルを使用し、既存の設計とテストを確認する。
3. 期待する振る舞いを示す pytest を先に追加または更新し、失敗することを確認する
   （RED）。
4. 最小限の実装でテストを通す（GREEN）。リファクタリング後もテストを通す。
5. 実装・仕様・テストを同じ変更として見直し、検証コマンドを実行する。

表示だけの小さな調整でも、ゲームの振る舞い・UI 要件・受け入れ条件に影響するなら
仕様を先に更新します。`overview.md` は企画の基準、`specs/mvp-game/spec.md` は現行 MVP の
実装上の正本です。両者に差がある場合は、勝手に企画を実装せず、仕様を明示的に更新します。

## 依存関係とライセンス

- Python ライブラリの追加・更新は必ず `uv add <package>`（開発用は
  `uv add --group dev <package>`）で行う。`pyproject.toml` と `uv.lock` を手編集して
  依存関係を追加しない。
- 追加するライブラリ、画像、フォント、音声、コード断片は、商用利用・再配布が可能な
  ライセンスであることを導入前に確認し、必要な帰属表示・LICENSE ファイルを残す。
- ライセンスが不明、非商用限定、または利用条件が本プロジェクトに適合しない素材は使わない。
- AWS のサービス名やロゴを扱う際は AWS の商標ガイドラインを確認する。公式ロゴを無断で
  素材化せず、必要なら独自の汎用アイコンを用いる。

## 実装上の注意

- ルールと状態遷移は `game.py` に置き、`main.py` にゲームロジックを重複させない。
  `game.py` は Flet に依存しないよう保つ。
- ランダムな挙動は `random.Random` を注入可能にして、テストで再現できるようにする。
- クレジットは常に 0 以上、アイテム所持数は最大 3、支払日は「停止時・通過時」双方で
  処理する。これらの不変条件を壊さない。
- クイズは `assets/quiz.md` と `assets/quiz-architecture.md` のデータを利用し、同じデッキを
  使い切るまで重複出題しない。
- イベントメッセージは固定文言にせず、既存のランダムメッセージプールの方針を保つ。
- Flet は現行 API を使う。`ft.run`、`ft.Button`、`ft.Alignment`、`ft.BoxFit` を使用し、
  非推奨の `ft.app`、`ElevatedButton`、`ImageFit` を再導入しない。
- UI変更では、開始時の全体俯瞰、ターン中のトークン追従カメラ、マス間の非重複、
  サイコロ・移動・イベントの演出を損なわない。
- 既存のアセットとユーザーの変更を無断で削除・上書きしない。

## 検証

変更後は最低限、リポジトリ直下で次を実行する。

```powershell
uv run pytest -q
uv run ruff check .
uv run python -Wd -c "import main; print('main import ok')"
```

ゲームルールを変更した場合は `tests/test_game.py`、GUI API を変更した場合は
`tests/test_main.py` を更新する。可能な場合は実際に起動し、開始画面・サイコロ・移動・
カットイン・クイズ・アイテムを手動確認する。
